# Metas Tab Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Substituir a aba "Cartão" pela aba "Metas" no menu de navegação inferior.

**Architecture:** Alteração de um único item no array `NAV_ITEMS` em `BottomNav.tsx`. A `GoalsPage` e a rota `/goals` já existem; apenas o link de navegação está faltando.

**Tech Stack:** React, TypeScript, React Router v6, Tailwind CSS

---

## Files

- Modify: `kekel-finance/src/components/layout/BottomNav.tsx` (linha 8)

---

### Task 1: Substituir aba Cartão por Metas no BottomNav

**Files:**
- Modify: `kekel-finance/src/components/layout/BottomNav.tsx`

- [ ] **Step 1: Abrir o arquivo e localizar o item Cartão**

  Arquivo: `kekel-finance/src/components/layout/BottomNav.tsx`, linha 8:
  ```ts
  { to: '/card', icon: '💳', label: 'Cartão'   },
  ```

- [ ] **Step 2: Substituir o item**

  Trocar pela linha:
  ```ts
  { to: '/goals', icon: '🎯', label: 'Metas'   },
  ```

  O array final deve ficar:
  ```ts
  const NAV_ITEMS = [
    { to: '/',         icon: '🏠', label: 'Início'   },
    { to: '/income',   icon: '💰', label: 'Renda'    },
    { to: '/expenses', icon: '💸', label: 'Gastos'   },
    { to: '/fixed',    icon: '📋', label: 'Fixos'    },
    { to: '/goals',    icon: '🎯', label: 'Metas'    },
  ]
  ```

- [ ] **Step 3: Verificar lint e typecheck**

  ```bash
  cd kekel-finance && npm run lint && npm run typecheck
  ```
  Esperado: sem erros.

- [ ] **Step 4: Verificar build**

  ```bash
  npm run build
  ```
  Esperado: build concluído sem erros.

- [ ] **Step 5: Commit**

  ```bash
  git add kekel-finance/src/components/layout/BottomNav.tsx
  git commit -m "feat: replace Cartão tab with Metas in BottomNav"
  ```
